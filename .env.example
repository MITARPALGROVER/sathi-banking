# Sathi Digital Companion — Environment Configuration

# Custom OpenAI-compatible Provider Setup (e.g., DeepSeek, OpenRouter, local server, etc.)
# If using a custom base URL (e.g. DeepSeek or OpenRouter), set it here.
# Default if not set is the standard OpenAI base URL (https://api.openai.com/v1).
AI_API_KEY=your_api_key_here
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL=gpt-4o-mini

# Legacy Config (Fallback)
# LOVABLE_API_KEY=your_lovable_api_key_here
